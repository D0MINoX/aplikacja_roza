name: Build iOS App (.ipa)

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Rozwiązuje ostrzeżenie z Twojego screena
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x 

      - name: Install MAUI Workloads
        # Dodajemy 'android', żeby kompilator przestał zgłaszać błąd ze zdjęcia
        run: dotnet workload install maui-ios maui-android

      - name: Build and Publish (.ipa)
        run: |
          cd MauiApp1
          dotnet publish -f net10.0-ios -c Release /p:ArchiveOnBuild=true /p:RuntimeIdentifier=ios-arm64 /p:CodesignKey="-" /p:CodesignProvision="-"
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app-bundle
          path: MauiApp1/bin/Release/net10.0-ios/ios-arm64/publish/*.ipa
