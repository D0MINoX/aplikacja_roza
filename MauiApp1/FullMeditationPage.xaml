<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.FullMeditationPage"
             Title="FullMeditationPage">
    <Grid RowDefinitions="*,auto">
        <Grid RowDefinitions="auto,auto" Padding="20" Grid.Row="0" RowSpacing="10">
            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource Kafelki}" Padding="15">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10"/>
                </Border.StrokeShape>
                <Grid ColumnDefinitions="auto,auto">
                    <Image Grid.Column="0" Source="book_icon.png" HeightRequest="100" WidthRequest="100" VerticalOptions="Center"/>
                    <Label Grid.Column="1" TextColor="{StaticResource Text}" VerticalOptions="Center" FontSize="Large">
                        Rozważania różańcowe
                    </Label>
                </Grid>
            </Border>

            <!-- Całe rozważanie -->
            <Grid RowDefinitions="auto,auto" Grid.Row="1" RowSpacing="10">
                <Border Background="{StaticResource Kafelki}" Grid.Row="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>
                    <Grid RowDefinitions="auto,*,auto" RowSpacing="20" Padding="20" >
                        <!-- data -->
                        <Label Grid.Row="0" x:Name="DateLabel" InputTransparent="True"/>
                        <!-- tekst rozważania -->
                        <ScrollView Grid.Row="1" Orientation="Vertical" MaximumHeightRequest="400">
                            <Label  TextColor="{StaticResource Text}" VerticalOptions="Center" x:Name="FullMeditation" InputTransparent="True"/>
                        </ScrollView>
                       
                        <!-- przyciski -->
                        <Grid ColumnDefinitions="*,*" Grid.Row="2">
                            <Border Grid.Column="0" Background="{StaticResource Menu}" HorizontalOptions="Start" Padding="10" WidthRequest="100">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="BackTapped"/>
                                </Border.GestureRecognizers>
                                <Label TextColor="{StaticResource Text}" HorizontalOptions="Center" InputTransparent="True">
                                    Cofnij
                                </Label>
                            </Border>
                            <Border Grid.Column="1"
                                    Background="{StaticResource Menu}"
                                    HorizontalOptions="End"
                                    Padding="10"
                                    WidthRequest="100"
                                    Stroke="{StaticResource Outline}"
                                    StrokeThickness="3"
                                    x:Name="Complete">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="CompletedTapped"/>
                                </Border.GestureRecognizers>
                                <Label TextColor="{StaticResource Text}" HorizontalOptions="Center" InputTransparent="True">
                                    Ukończono
                                </Label>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>